#ifndef BRIDGERRHOLT_ALLOCATORS_CORRUPTION_TEST_0_BLOCK_LIST_H
#define BRIDGERRHOLT_ALLOCATORS_CORRUPTION_TEST_0_BLOCK_LIST_H

#include <vector>

#include <allocators/blocks/block.h>

namespace bridgerrholt {
	namespace allocators {

using BlockList = std::vector<RawBlock>;

	}
}

#endif
