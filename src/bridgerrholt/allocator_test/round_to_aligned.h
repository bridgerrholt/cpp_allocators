#ifndef BRIDGERRHOLT_ALLOCATOR_TEST_ROUND_TO_ALIGNED_H
#define BRIDGERRHOLT_ALLOCATOR_TEST_ROUND_TO_ALIGNED_H

#include "common_types.h"

namespace bridgerrholt {
	namespace allocator_test {

SizeType roundToAligned(SizeType size);

	}
}

#endif
